#
# NOTE: kolla manages prometheus external frontend  via enable_prometheus_server_external variable
#       This is now just an example and commented out
#

# frontend prometheus_server_front_custom
#     mode http
#     http-request del-header X-Forwarded-Proto
#     option httplog
#     option forwardfor
#     http-request set-header X-Forwarded-Proto https if { ssl_fc }
#     bind {{kolla_external_vip_address}}:{{prometheus_listen_port}} ssl crt /etc/haproxy/certificates/haproxy.pem alpn h2,http/1.1
#     default_backend prometheus_server_back
#
# backend prometheus_server_back_custom
#     mode http
#     {% for host in groups['monitoring'] %}
#     {% set host_name = hostvars[host].ansible_facts.hostname %}
#     {% set host_ip = 'api' | kolla_address(host) %}
#     server {{ host_name }} {{ host_ip }}:{{prometheus_listen_port}} check inter 2000 rise 2 fall 5
#     {% endfor %}
