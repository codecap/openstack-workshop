# openstack-workshop


## TODOs
* Install packages
```bash
apt install -y\
 vim \
 git \
 tcpdump \
 curl
```
* post-pve-install.sh
* install ubuntu image
* create template
* create bridges according to network_conf
* create vms according to server_conf
